/** microCMS list limit — home Web Design section */
export const HOME_WEB_DESIGN_LIMIT = 3;

/** microCMS list limit — home Web Development section */
export const HOME_WEB_DEVELOPMENT_LIMIT = 6;

/**
 * Photography API list entries on the home page.
 * One entry holds all category blocks; not the same as category or image count.
 */
export const HOME_PHOTOGRAPHY_ENTRY_LIMIT = 1;

/** Home Photography: categories shown on TOP */
export const HOME_PHOTO_CATEGORY_COUNT = 4;

/** Home Photography: images per category on TOP */
export const HOME_PHOTOS_PER_CATEGORY = 2;

/** Sub-pages: max items per microCMS list request */
export const SUBPAGE_MICROCMS_LIST_LIMIT = 100;

/** Expand category references on work APIs */
export const MICROCMS_CATEGORY_DEPTH = 1;
